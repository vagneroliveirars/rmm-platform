spring:
  datasource:
    url: 'jdbc:h2:mem:test_db;DB_CLOSE_ON_EXIT=TRUE'
    username: sa
    password: ''
    driver-class-name: 'org.h2.Driver'
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
